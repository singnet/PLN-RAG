fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
dspy-ai==3.1.3
dspy==3.1.3
openai>=1.0.0
spacy>=3.7.0
faiss-cpu>=1.8.0
numpy>=1.26.0
networkx>=3.0.0
hyperon==0.2.10
sexpdata>=1.0.2
httpx>=0.27.0
python-dotenv>=1.0.0
more-itertools>=10.0.0
langextract>=1.0.0
pytest>=8.0.0
pytest-cov>=5.0.0
# Pinned to a commit, not a branch: upstream HEAD moved to a hard dspy==3.2.1
# pin that conflicts with ours, which broke `docker compose build` while the
# already-built image kept working. This SHA is what the working image installed
# and it only requires dspy>=3.1.3.
git+https://github.com/rTreutlein/NL2PLN.git@549fba9891eda50c8d957a674bf2f5e1878e86ff
